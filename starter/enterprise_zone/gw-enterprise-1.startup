#This L3 switch aims to connect each zone's firewall
#

# eth2 to the distribution layer switches
ip addr add 192.168.102.1/24 dev eth2
ip link set up dev eth2

# eth30 to the FW
ip addr add 10.0.0.10/30 dev eth30
ip link set up dev eth30

ip route add default via 10.0.0.9